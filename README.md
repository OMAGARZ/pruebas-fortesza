# pruebas-fortesza
tener instalado CYPRESS con el comando npm init -y y despues el comando npm install cypress --save-dev  y el plugin que dejo acontinuacion esto teniendo como fin que el scrip no falle esto estando dentro de la carpeta elejida para gusrdar tus documentos
npm install -D cypress-xpath
una vez instalado el plugin cambiar los datos del correo y contraseña esto para que no arroje error al momento de hacer el registro cuando el registro sea exitoso cambiar en el segunto it a estos valores dejdos por  usted para que el segundo it corra con sus datos de registro
